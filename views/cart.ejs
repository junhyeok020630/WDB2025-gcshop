<!--202136049 최준혁-->
<form method="post">
    <table class="table table-bordered border-primary table-hover" style="text-align: center;">
        <thead class="table-primary "><th>구매선택</th><th>상품</th><th>상품명</th><th>단가</th><th>담은날</th><th>수량</th>
        </thead> 
        <% for (var i = 0; i < carts.length; i++) { %>
        <tr>
            <td><input type="checkbox" name="selected" value="<%= carts[i].prod_id %>" ></td>
            <td><img src="<%= carts[i].image %>" style="width:100px;height:100px; "></td>
            <td><%= carts[i].name %></td>
            <td>
                <%= carts[i].price.toLocaleString('ko-KR') %> 원
            </td>
            <td><%= carts[i].date %></td>
            <td>
                <input type="number" name="qty" value = "1" min = "1" max = "<%= carts[i].stock %>">
                <p>남은 수량 : <%= carts[i].stock %></p>
            </td>

        </tr>
        <% } %>
    </table>
    <% if (carts.length == 0) { %>
        <p>장바구니에 담긴 상품이 없습니다.</p>
    <% } %>
    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <button class="btn btn-outline-primary btn-sm" formaction="/purchase/cart_purchase" type="submit" >결재</button></a>
        <button class="btn btn-outline-primary btn-sm" formaction="/purchase/cart_delete_process" type="submit" >삭제</button></a>
        <a href="/" ><button class="btn btn-outline-primary btn-sm" type="button" >상품 목록 돌아가기</button></a>
    </div>   
</form>

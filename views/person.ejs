<!--202136049-->
<table class="table table-bordered border-primary table-hover" style="text-align: center;">
    <thead class="table-primary "><th>ID</th><th>PW</th><th>이름</th><th>성별</th><th>주소</th><th>전화번호</th><th>생일</th><th>클래스</th><th colspan="2">메뉴</th>
    </thead> 
    <% for (var i = 0; i < person.length; i++) { %>
    <tr>
        <td><%- person[i].loginid %></td>
        <td><%- person[i].password %></td>
        <td><%- person[i].name %></td>
        <% if (person[i].mf == 'm') { %>
            <td>남자</td>
        <% } else { %>
            <td>여자</td>
        <% } %>
        <td><%- person[i].address %></td>
        <td><%- person[i].tel %></td>
        <td><%- person[i].birth %></td>
        <% if (person[i].class === 'CST') { %>
            <td>CST(고객)</td>
        <% } else if (person[i].class === 'MNG') { %>
            <td>MNG(관리자)</td>
        <% } else if (person[i].class === 'CEO') { %>
            <td>CEO(경영자)</td>
        <% } else { %>
            <td>NON(미등록)</td>
        <% } %>
        <% if (cls == 'MNG') { %>
            <td><a href="/person/update/<%- person[i].loginid %>" style="text-decoration: none;">수정</a></td>
            <td><a href="/person/delete/<%- person[i].loginid %>" style="text-decoration: none;" onclick="if(confirm('정말로 삭제하시겠습니까?')==false) return false">삭제</a></td>
        <% } %>
    </tr>
    <% } %>
</table>
<% if (person.length == 0) { %>
    <p>등록된 회원이 없으니 회원 추가 버튼으로 회원을 추가해 주십시오. </p>
<% } %>
<a href="/person/create"><button class="btn btn-outline-secondary btn-sm" type="submit">회원 추가</button></a>